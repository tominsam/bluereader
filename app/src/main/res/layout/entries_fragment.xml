<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <data>

        <variable
            name="viewType"
            type="org.movieos.feeder.model.Entry.ViewType"
            />

        <import type="org.movieos.feeder.model.Entry.ViewType"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        >

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            style="?toolbarStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:title="@string/app_name"
            />

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            >

            <android.support.v4.widget.SwipeRefreshLayout
                android:id="@+id/swipe_refresh_layout"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                >

                <android.support.v7.widget.RecyclerView
                    android:id="@+id/recycler_view"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:layoutManager="android.support.v7.widget.LinearLayoutManager"
                    />
            </android.support.v4.widget.SwipeRefreshLayout>

            <FrameLayout
                android:id="@+id/empty"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/background"
                >

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:gravity="center"
                    android:text="No entries"
                    android:textAppearance="@style/text_title"
                    />
            </FrameLayout>
        </FrameLayout>

        <LinearLayout
            style="?toolbarStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            >

            <ImageButton
                android:id="@+id/state_starred"
                style="@style/ToolbarButton"
                android:contentDescription="View starred entries"
                android:src="@drawable/selectable_starred"
                app:selected="@{viewType == ViewType.STARRED}"
                />

            <ImageButton
                android:id="@+id/state_unread"
                style="@style/ToolbarButton"
                android:layout_marginEnd="32dp"
                android:layout_marginStart="32dp"
                android:contentDescription="View unread entries"
                android:src="@drawable/selectable_unread"
                app:selected="@{viewType == ViewType.UNREAD}"
                />

            <ImageButton
                android:id="@+id/state_all"
                style="@style/ToolbarButton"
                android:contentDescription="View all entries"
                android:src="@drawable/selectable_all"
                app:selected="@{viewType == ViewType.ALL}"
                />

        </LinearLayout>


    </LinearLayout>
</layout>
